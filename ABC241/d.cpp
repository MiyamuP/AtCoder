#include <iostream>
#include <vector>
#include <list>
using namespace std;
#define ll long long
#define rep(i, n) for (ll i = 0; i < n; i++)

int main()
{
	ll q;
	cin >> q;
	rep(i, q)
	{
		ll
	}
	return 0;
}