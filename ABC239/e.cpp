#include <iostream>
#include <vector>
using namespace std;
#define ll long long
#define rep(i, n) for (ll i = 0; i < n; i++)

int main()
{
	ll n, q;
	cin >> n >> q;
	vector<ll> x(n);
	rep(i, n)
	{
		cin >> x[i];
	}
	rep(i, n)
	{
	}

	return 0;
}