#include <iostream>
using namespace std;
#define ll long long
#define rep(i, n) for (ll i = 0; i < n; i++)
ll dp[100001][5];
int main()
{
	ll n;
	cin >> n;
	ll t[n], x[n], a[n];
	rep(i, n)
	{
		cin >> t[i] >> x[i] >> a[i];
	}
	rep(i, 5)
	{
		if ()
			dp[0][i] = 0;
	}

	ll max = 0;
	rep(i, n)
	{
		rep(j, 5)
		{
		}
	}
	return 0;
}