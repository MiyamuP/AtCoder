#include <iostream>
#include <set>
#include <map>
using namespace std;
#define ll long long
#define rep(i, n) for (ll i = 0; i < n; i++)
int main()
{
	ll n;
	cin >> n;
	ll l[n], r[n];
	set<ll> lis;
	map<ll, ll> ram;
	rep(i, n)
	{
		cin >> l[i] >> r[i];
		auto itr = 
	}
	return 0;
}