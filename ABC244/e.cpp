#include <iostream>
using namespace std;
#define ll long long
#define rep(i, n) for (ll i = 0; i < n; i++)
ll div = 998244353;
int main()
{
	ll n, m, k, s, t, x;
	cin >> n >> m >> k >> s >> t >> x;
	ll u[m], v[m];
	rep(i, m)
	{
		cin >> u[i] >> v[i];
	}
	return 0;
}